# V001 core implementation

Reserved for this method's adaptation/model wrapper and run orchestration.
No model-adaptation implementation exists yet. Do not copy the Omega provider or silently use inherited
`training/` as this method. Register parameterization and the optimization protocol require a separate implementation task.
