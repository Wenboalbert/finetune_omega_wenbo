# Inherited legacy code: reference only on v001-focal-only-register-token

This directory was inherited from the legacy training branch and is not the V001 implementation.
Do not launch or re-enable its PBS scripts here: they point at the sibling legacy checkout.
Do not use its pose/FOV supervised losses as focal-only adaptation. New core work belongs in ../src/.
Read ../AGENTS.md and ../README_V001.md. For explicitly requested legacy training, operate its own checkout.
